<h1>CLASSI CHE FANNO <%= @subject.name %></h1>

<div class="transition_link">
  <ul class="horizontal">
    <% @courses.each do |c| %>
      <li>
      <%= link_to c.name, course_path(id: c, from_subject: @subject.id) %>
      </li>
    <% end %>
  </ul>
</div>

<div class = "back_link">
	<%= link_to "Indietro alla materia #{@subject.name}", subject_path(@subject) %>
</div>