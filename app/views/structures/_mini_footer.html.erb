<div class="group_link">
	<div>
		<ul class="horizontal">
 			<li>
    			<%= link_to 'Tutte le strutture', structures_path %>
    		</li>
  		</ul>
  	</div>
  	<div>
  		<ul class="horizontal">
    		<li>
     			<%= link_to "<<Prev", structure_path(@structures[@prev].id, from_guided_tour: true) if @prev_show %>
    		</li>
    		<li>
     			<%= link_to "Next>>", structure_path(@structures[@next].id, from_guided_tour: true) if @next_show %>
   			</li>
  		</ul>
  	</div>
</div>

<div class = "back_link">
	<%= link_to "Torna all'evento #{@from_event.name}", event_path(@from_event_id) if @from_event_id %>
</div>