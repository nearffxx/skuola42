<% provide(:title, 'STRUTTURE') %>

<h1>STRUTTURA: <%= @structure.name %></h1>

<div class="image">
  <%= image_tag("/structures/#{@structure.img_url}") %>
</div>

<p class="description_topic"><%= @structure.description %></p>

<div class="transition_link">
  <ul class="horizontal">
    <li>
      <%= link_to 'Eventi ospitati', show_events_structure_path(@structure) %>
    </li>
  </ul>
</div>

<%= render 'structures/mini_footer' %>